//! Legacy FFmpeg pipe module.
//!
//! Video encoding is now handled by `FfmpegVideoEncoder` in `backend/ffmpeg.rs`
//! via the `MediaEncoder` trait. This module is kept for the `VideoCodec` and
//! `ExportConfig` types re-exported from `export/mod.rs`.
